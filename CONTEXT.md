# General
The project consist of two parts: 
- Docker container using playwright to do simple scraping operation, loading page and then extracting value(s) using provided selector. 
- Use case specific (specialized) scripts that are mounted to container instead of build-in script and provide specialized services.